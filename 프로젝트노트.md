# 프로젝트 노트

## [시리즈 일괄등록 정의]

상품 DB 하단 선택상품 일괄처리에 `선택상품 시리즈 생성`을 추가한다.
개별 상품 시리즈/연관그룹 등록(`prd_detail_relation_group`, `relation_group_management`)과 같은 `prd_relation_group` / `prd_relation_group_product` 구조를 사용한다.
이 기능은 시리즈(`prg_mode = series`)만 처리한다. 특정 그룹(`custom_group`) 일괄등록은 포함하지 않는다.

### 화면

- 위치: 상품 DB 목록 하단 (`resources/views/admin/product/product_db.php`)
- 버튼: `선택상품 시리즈 생성`
- 처리 방식: `새 시리즈 생성` / `기존 시리즈에 추가`
- 새 시리즈: 시리즈 이름(필수), 관리 메모(선택)
- 기존 시리즈: 같은 브랜드의 활성 시리즈 선택
- 모달에 선택 상품 수와 공통 브랜드를 표시한다.

### 공통 규칙

1. 같은 브랜드만 등록 가능
   - 선택 상품의 브랜드1/브랜드2 교집합이 있어야 한다.
   - 교집합이 없으면 등록하지 않는다.
   - 새 시리즈는 대표브랜드가 모두 같고 그 값이 교집합에 있으면 그 브랜드로 만든다.
   - 기존 시리즈는 시리즈 브랜드가 교집합에 있을 때만 추가한다.

### 새 시리즈 생성

1. 동일 시리즈 이름 생성 불가
   - `prg_mode = series` 기준으로 이름이 이미 있으면 생성하지 않는다.
   - 브랜드가 달라도 시리즈 이름은 중복될 수 없다.
   - 개별 시리즈 생성/저장에도 같은 이름 규칙을 적용한다.
2. 이미 시리즈에 등록된 상품은 제외
   - 어떤 시리즈든 이미 포함된 상품은 새 시리즈 생성에서 빼는다.
   - 특정 그룹만 있는 상품은 시리즈 생성 대상에 포함한다.
   - 제외된 상품 이름과 기존 시리즈 이름을 결과에 알려준다.
   - 선택 상품이 모두 제외되면 빈 시리즈를 만들지 않는다.

### 기존 시리즈에 추가

1. 같은 브랜드의 사용 중인 시리즈만 선택할 수 있다.
2. 이미 해당 시리즈에 있는 상품만 제외한다.
   - 다른 시리즈에 있는 상품도 이 시리즈에 추가할 수 있다.
   - 제외된 상품 이름과 기존 시리즈 이름을 결과에 알려준다.
3. 선택 상품이 모두 이미 해당 시리즈에 있으면 추가하지 않는다.

### 처리

- action: `bulk_create_product_series`
- `series_mode=create` 또는 `series_mode=existing`
- 기존 추가 시 `prg_idx`를 함께 보낸다.
- 엔드포인트: `/admin/product/action`
- 서비스: `ProductService::bulkCreateProductSeries()`
- 대상 상품을 시리즈에 포함하고 관리 로그를 남긴다.

### 이후 일괄처리

상품 DB 하단 선택상품 일괄처리 버튼은 이후에도 더 추가한다.
새 일괄처리 기능을 넣을 때도 이 화면의 선택 체크박스(`check_idx[]`)와 같은 선택 집합을 사용한다.
