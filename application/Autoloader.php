<?php

/*
spl_autoload_register(function ($class_name) {
    $file = __DIR__ . "/classes/" . $class_name . ".php";
    if (file_exists($file)) {
        require_once $file;
    } else {
        throw new Exception("클래스 파일 {$class_name}.php를 찾을 수 없습니다.");
    }
});

*/